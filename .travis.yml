language: ruby
rvm:
- 2.2
script: jekyll build
branches:
  only:
  - travis
after_success:
    cd ./_site && find -type f -exec curl -u $FTP_USER_BRANDONIOUS:$FTP_PASS_BRANDONIOUS --ftp-create-dirs -T {} ftp://brandonious.com{} \;